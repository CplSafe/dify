"""Asset Library controllers — files, prompts, generic CRUD.

Routes are registered as side effects when the modules under this package
are imported by ``controllers.console.__init__`` (see RESOURCE_MODULES /
explicit imports).
"""
